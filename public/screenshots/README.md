# Screenshots

Drop iPhone screenshots here. Recommended sizes:

- `radar.png` — main radar screen (used in feature row 01)
- `streaks.png` — streak / gentle tolerance screen (feature row 02)
- `metrics.png` — Consistency + Balance card screen (feature row 03)
- `heatmap.png` — calendar heatmap screen (feature row 04)
- `today.png` — today / dashboard screen (hero or social proof)
- `paywall.png` — paywall mirror (pricing section)
- `og.png` — 1200×630 social-share image for `<meta property="og:image">`

The landing page currently uses inline SVG mocks. Replace the `FeatureRow`
visual slots in `src/components/FeatureHighlights.astro` with `<img>` tags
once these files exist.
